---
description: "Synchronize OpenCode configuration from ~/.opencode/ to /home/vincent/dev/config_opencode/ backup repository"
---

You are a configuration sync specialist. Execute the following command and report the results:

```bash
~/.opencode/bin/sync-config $ARGUMENTS
```

Report the output, any errors, and suggest next actions if needed.
